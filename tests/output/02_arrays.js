import { __print } from './__util.js';
const nums = [10, 20, 30];
const first = nums[0];
const strings = ["Hello", "World"];
__print(`Nums: ${nums}`);
__print(`First: ${first}`);
__print(`Strings: ${strings}`);

import { __print } from '../../output/__util.js';
import * as __std_array from '../../output/std/array.js';
function main() {
  const arr = [10, 20, 30];
  __print(`Sum from extension: ${_ext_array_number_sum(arr)}`);
  __print(arr.length);
}
main();

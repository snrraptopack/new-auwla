/// A representation of types exclusively used during the inference phase.
/// Includes strict separation between `Concrete` types and `TypeVars` to
/// allow for constraint solving.
#[derive(Debug, Clone, PartialEq)]
pub enum InferredType {
    // To be implemented.
}
